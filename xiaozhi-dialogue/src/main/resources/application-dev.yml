# ============================================================
# Dialogue 开发环境配置
# ============================================================

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://yourAddress:3307/xiaozhi?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: root
    password: YourPassword

  data:
    redis:
      host: yourAddress
      port: 6379
      password: YourPassword

